const form=document.getElementById('adminLoginForm'),msg=document.getElementById('msg');form.addEventListener('submit',async e=>{e.preventDefault();msg.textContent='جارٍ التحقق...';try{const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},credentials:'same-origin',body:JSON.stringify({email:email.value,password:password.value,twoFactorCode:twoFactorCode.value})});const d=await r.json();if(!r.ok)throw new Error(d.message||'فشل تسجيل الدخول');if(!['super_admin','admin','support','finance','auditor','content_manager'].includes(d.user?.role))throw new Error('هذا الحساب ليس حساب إدارة.');location.href='/admin/dashboard';}catch(e){msg.textContent=e.message}});
