const User=require('../models/User'); const Investment=require('../models/Investment'); const InvestmentPlan=require('../models/InvestmentPlan'); const Transaction=require('../models/Transaction');
async function getDashboard(){const [users,investments,plans,transactions,deposits,withdrawals]=await Promise.all([User.countDocuments(),Investment.countDocuments(),InvestmentPlan.countDocuments(),Transaction.countDocuments(),Transaction.aggregate([{$match:{type:'deposit',status:'completed'}},{$group:{_id:null,total:{$sum:{$toDecimal:'$amount'}}}}]),Transaction.aggregate([{$match:{type:'withdrawal',status:'completed'}},{$group:{_id:null,total:{$sum:{$toDecimal:'$amount'}}}}])]);return {users,investments,plans,transactions,totalDeposits:deposits[0]?.total?.toString()||'0',totalWithdrawals:withdrawals[0]?.total?.toString()||'0'};}
module.exports={getDashboard};
